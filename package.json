{
  "name": "ytllm",
  "version": "1.0.0",
  "description": "YouTube AI Chat Agent — monorepo root",
  "private": true,
  "scripts": {
    "dev:server": "cd server-python && pip install -r requirements.txt && python -m uvicorn app.main:app --reload --port 3001",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:client": "npm run build --prefix client",
    "install:all": "npm install --prefix client && cd server-python && pip install -r requirements.txt",
    "install:server:py": "cd server-python && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
