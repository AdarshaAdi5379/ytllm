# KnowledgeOS — Environment Variables
#
# THIS FILE IS STALE — it references the old Google/Gemini setup.
# Use backend/.env.example instead, which has the correct OpenAI-based config.
# 
# Copy backend/.env.example to backend/.env and configure your keys there.
