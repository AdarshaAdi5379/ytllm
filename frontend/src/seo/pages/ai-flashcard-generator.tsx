import { SeoPage } from '../SeoPage';
import { aiFlashcardGeneratorContent } from '../seo-content';

export default function AiFlashcardGeneratorPage() {
  return <SeoPage content={aiFlashcardGeneratorContent} />;
}
